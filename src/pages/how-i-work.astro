---
import BaseLayout from '../layouts/BaseLayout.astro';
import CTABand from '../components/sections/CTABand.astro';
import { asset, route } from '../lib/paths';
import { landscapeImages, portraitImages } from '../content/data';

const landscapePreview = landscapeImages.slice(0, 3);
---

<BaseLayout
  title="How I Work"
  description="Portrait photography rooted in collaboration, not direction. Based in Lafayette, IN and Urbana-Champaign, IL. Learn about the approach, who it suits, and what sessions are for."
>

  {/* ══ PAGE HEADER ══════════════════════════════════════════════════ */}
  <section class="page-header">
    <div class="container">
      <span class="eyebrow">How I work</span>
      <h1>The approach</h1>
      <p class="lead" style="margin-top: var(--space-4); max-width: 52ch;">
        Portrait photography shaped by collaboration, patience, and a background
        in nature and street photography. Here's what that means in practice.
      </p>
    </div>
  </section>


  {/* ══ NOT EVERY PHOTOGRAPHER ═══════════════════════════════════════
      Split: text left, dancer portrait right.
      The dancer's dark stage background bleeds into bg-strong intentionally.
  ══════════════════════════════════════════════════════════════════ */}
  <section class="section bg-strong">
    <div class="container">
      <div class="portrait-split">
        <div class="stack">
          <span class="eyebrow">I'm not from round here...</span>
          <h2>Not every photographer will tell your story.</h2>
          <p>
            There's a particularly Midwest kind of portrait photography:
            golden hour, cornfields, soft focus, saying: "Cheese!" (Or
            maybe sometimes: "TRACTOR!") It can be beautiful and many
            photographers excel at making these portraits.
          </p>
          <p class="prose-punch">I'm not one of those.</p>
          <p>
            My photography background is in portrait work, nature, and street
            photography — alongside about twenty years in marketing and brand
            content strategy. I've learned to capture what makes something its
            own: its story, context, culture and purpose.
          </p>
          <p>
            I work with people to find something deeply personal and
            intentional. I can make images for professional or personal brands,
            or celebrate meaningful milestones. My favourite kind of portrait
            is made when someone is exploring just for themself.
          </p>
          <p>We start with exploration, and work towards expression.</p>
        </div>
        <div class="split-image worth-knowing-image">
          <img
            src={asset(portraitImages[18].src)}
            alt={portraitImages[18].alt}
            width={portraitImages[18].width}
            height={portraitImages[18].height}
            loading="lazy"
            style="object-position: center top"
          />
        </div>
      </div>
    </div>
  </section>


  {/* ══ FOR YOUR BRAND, FAMILY, OR YOURSELF ══════════════════════════
      Split: image left (baby.jpg), text right.
  ══════════════════════════════════════════════════════════════════ */}
  <section class="section">
    <div class="container">
      <div class="split">
        <div class="split-image">
          <img src={asset('portrait/baby.jpg')} alt="Portrait — child's hands on a ukulele, close crop" width="2000" height="1334" loading="lazy" />
        </div>
        <div class="stack">
          <span class="eyebrow">What sessions are for</span>
          <h2>For your brand, your family, or just yourself</h2>
          <p>
            Practically, these sessions produce work you can use — faculty pages,
            research profiles, professional and social media content, a modeling portfolio.
          </p>
          <p>
            But I'm equally interested in what sits outside the practical: someone
            exploring how they want to be seen; a couple or family making something
            true about who they actually are. Queer families, blended families,
            people who've never found a photographer who saw them right — these
            are exactly the stories I want to tell.
          </p>
          <p>The result is something specific, a bit unexpected, and expressively yours.</p>
        </div>
      </div>
    </div>
  </section>

  {/* ══ OPEN TO EXPLORING ═══════════════════════════════════════════
      Split: film image left, text right.
  ══════════════════════════════════════════════════════════════════ */}
  <section class="section">
    <div class="container">
      <div class="split">
        <div class="split-image">
          <img
            src={asset('film/IMG_0990.JPG')}
            alt="Film photograph — Minolta 35mm"
            width="1418"
            height="2108"
            loading="lazy"
          />
        </div>
        <div class="stack">
          <span class="eyebrow">Open to more</span>
          <h2>I'm exploring, too.</h2>
          <p>
            If you've got something on your mind — even the very stub of an artistic idea —
            let's talk. I'm open to experiment and work on translating imagination into imagery.
          </p>
          <p>
            Costume, location, style... we can collaborate on an artistic project and help get
            your concepts down onto paper (or up on a screen). I'm especially interested in
            working with fashion designers, makeup artists, and artists who want to bring
            photography into the mix.
          </p>
        </div>
      </div>
    </div>
  </section>


  {/* ══ LANDSCAPE & ABSTRACT TEASER ══════════════════════════════════ */}
  <section class="section bg-strong">
    <div class="container">
      <div class="section-header">
        <span class="eyebrow">Beyond portraits</span>
        <h2>Landscape &amp; abstract work</h2>
        <p class="lead" style="margin-top: 1rem;">
          The same eye I bring to portraits — attention to light, texture, and mood —
          applied to the natural and abstract world.
        </p>
      </div>
      <div class="landscape-preview">
        {landscapePreview.map(img => (
          <div class="lp-item">
            <img src={asset(img.src)} alt={img.alt} width={img.width} height={img.height} loading="lazy" />
          </div>
        ))}
      </div>
      <div style="margin-top: var(--space-6);">
        <a href={route('portfolio/landscape/')} class="btn btn-outline">See landscape &amp; abstract work</a>
      </div>
    </div>
  </section>


  {/* ══ CTA ═══════════════════════════════════════════════════════════ */}
  <CTABand
    eyebrow="Get in touch"
    heading="Let's start a conversation"
    body="Tell me what you're thinking — the ideas you have, what you want to explore, what you want to make. Then we can talk."
    primaryHref={route('contact/')}
    primaryLabel="Get in touch"
    secondaryHref={route('sessions/')}
    secondaryLabel="See sessions & pricing"
  />

</BaseLayout>

<style>
  /* Typographic punch line — page-specific weight */
  .prose-punch {
    font-size: var(--text-xl);
    font-weight: 600;
  }

  /* On mobile, float the dancer image above the text block */
  @media (max-width: 960px) {
    .worth-knowing-image { order: -1; aspect-ratio: 4 / 3; }
  }

  /* Landscape preview strip — used only on this page */
  .landscape-preview {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: var(--space-3);
    margin-top: var(--space-7);
  }
  .lp-item {
    aspect-ratio: 4 / 3;
    overflow: hidden;
    border-radius: var(--radius-lg);
  }
  .lp-item img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    display: block;
  }

  @media (max-width: 960px) {
    .landscape-preview { grid-template-columns: 1fr 1fr; }
    .landscape-preview .lp-item:last-child { display: none; }
  }
</style>
